/**
 * DynamicIsland mode definitions
 */

export const IslandModes = {
  COMPACT: 'compact',       // Small pill, shows count
  PROXIMITY: 'proximity',   // Expanded, shows nearby people
  NOTIFICATION: 'notification' // Temporary, shows notification
};
